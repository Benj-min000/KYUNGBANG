rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  	// Match the user document
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

			// Matching the subcollection
      match /userAddress/{addressId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
